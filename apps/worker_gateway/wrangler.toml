name = "pixiv2kindle-gateway"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# ── Secrets (set via `wrangler secret put`) ────────────
# DISCORD_PUBLIC_KEY
# DISCORD_APPLICATION_ID
# ALLOWED_DISCORD_USER_ID
# INTERNAL_API_TOKEN
# BACKEND_BASE_URL

[vars]
# Non-secret env vars can go here for dev/staging.
# Production secrets should use `wrangler secret put`.
